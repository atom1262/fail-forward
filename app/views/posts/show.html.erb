<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Industry:</strong>
  <%= @post.industry.name %>
</p>

<p>
  <strong>Posted by:</strong>
  <img src="<%= @post.user.picture_url %>">
  <%= @post.user.formatted_name %>
<p>
  <strong>Text:</strong>
  <%= @post.body %>
</p>

<%= link_to 'Back', posts_path %>
<%= link_to 'Edit Post', edit_post_path(@post) %>
<%= link_to 'Delete Post', @post, method: :delete %>

<h3>Comments</h3>

<%= form_for ([@post, @comment]) do |f| %>
  <%= f.text_area :body, placeholder: 'Type Comment' %>
  <%= f.submit 'Submit Comment' %>
<% end %>

<div>
  <% @comments.each do |comment| %>
    <ul>
      <li><%= comment.body %></li>
      <img src="<%= comment.user.picture_url %>"><%= comment.user.formatted_name %> said <%= time_ago_in_words(comment.created_at) %> ago
    </ul>
  <% end %>
</div>

