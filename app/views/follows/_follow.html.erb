<li>
  <%= link_to follow.post.title, follow.post %> <%= pluralize(follow.post.comments.count, 'comment') %>
  <% if !current_user.owns?(follow.post) %>
    submitted by <%= link_to follow.post.user_full_name, user_path(follow.post.user) %>
  <% end %>
</li>
